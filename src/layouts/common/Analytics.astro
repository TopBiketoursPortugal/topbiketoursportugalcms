---
import { GoogleAnalytics } from '@astrolib/analytics';
import { GOOGLE_ANALYTICS_ID } from 'astro:env/client';
---

{
  GOOGLE_ANALYTICS_ID ? (
    <GoogleAnalytics id={String(GOOGLE_ANALYTICS_ID)} partytown />
  ) : null
}
