---
import SocialMenu from './SocialMenu.astro';
import Contact from 'src/../data/contact.json';
import Icon from 'src/components/utility/Icon.1';
import type { LanguageCodes } from 'src/schemas/language';

type Props = {
  language: LanguageCodes;
};

const { language = 'en' } = Astro.props;
const { phone, email } = Contact[language];
---

<div
  class="bg-black text-gray-300 relative hidden justify-between gap-1 overflow-hidden text-ellipsis whitespace-nowrap px-3 py-2 text-sm dark:border-slate-800 dark:border-b dark:bg-slate-950 dark:bg-transparent md:flex"
>
  <div class="text-white flex gap-4 dark:text-slate-400">
    <a href="/" class="flex flex-row items-center justify-start gap-2">
      <Icon icon="ph:phone-thin" />
      <span class="whitespace-nowrap">{phone}</span>
    </a>
    <a
      href={'mailto:' + email}
      class="flex flex-row items-center justify-start gap-2"
    >
      <Icon icon="ph:envelope-simple-thin" /><span class="whitespace-nowrap">
        {email}
      </span>
    </a>
  </div>
  <SocialMenu class:list="flex" />
</div>
