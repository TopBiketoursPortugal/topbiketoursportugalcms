---
import type { IconNames } from 'src/types';
import { cn } from 'utils/cn';
import { file, type SymbolId } from 'virtual:astro-svgs';

type Props = {
  icon: IconNames;
  className?: string;
  class?: string;
  title?: string;
  slot?: string;
};

const { icon, className, class: className2, title } = Astro.props;
const style = cn('h-6 w-6 fill-current', className, className2);

/**
 * Convert icon name to astro-svgs symbolId format
 * astro-svgs uses just the filename (without directory) as the symbol ID
 * Examples:
 *   'ph:sun-light' -> 'sun-light'
 *   'logos:visa' -> 'visa'
 *   'circle-flags:pt' -> 'pt'
 */
function getSymbolId(iconName: IconNames): SymbolId {
  // Legacy icon name mappings
  const legacyMappings: Record<string, SymbolId> = {
    Envelope: 'envelope-simple-light' as SymbolId,
    User: 'user-light' as SymbolId,
    Bread: 'bread-light' as SymbolId,
    Mug: 'coffee-light' as SymbolId,
    Egg: 'egg-light' as SymbolId,
    Utensils: 'fork-knife-light' as SymbolId,
    Carrot: 'carrot-light' as SymbolId,
    Burger: 'hamburger-light' as SymbolId,
    Fish: 'fish-simple-light' as SymbolId,
    Seedling: 'plant-light' as SymbolId,
    PlateWheat: 'bowl-food-light' as SymbolId,
    PizzaSlice: 'pizza-light' as SymbolId,
    PepperHot: 'pepper-light' as SymbolId,
    ChampagneGlasses: 'wine-light' as SymbolId,
    Check: 'check-light' as SymbolId,
    CircleCheck: 'check-circle-fill' as SymbolId,
    ArrowRight: 'arrow-right-light' as SymbolId,
    ArrowLeft: 'arrow-left-light' as SymbolId,
    ArrowDown: 'arrow-down-light' as SymbolId,
    ArrowUp: 'arrow-up-light' as SymbolId,
    CaretLeft: 'caret-left-light' as SymbolId,
    CaretRight: 'caret-right-light' as SymbolId,
    CaretDown: 'caret-down-light' as SymbolId,
    CaretUp: 'caret-up-light' as SymbolId,
    PaperPlane: 'paper-plane-light' as SymbolId,
    CartShopping: 'shopping-cart-light' as SymbolId,
    Shop: 'storefront-light' as SymbolId,
    BagShopping: 'shopping-bag-light' as SymbolId,
    CreditCard: 'credit-card-light' as SymbolId,
    Store: 'storefront-light' as SymbolId,
    ShopLock: 'lock-light' as SymbolId,
    Car: 'car-light' as SymbolId,
    CircleHalfStroke: 'circle-half-fill' as SymbolId,
    LocationDot: 'map-pin-light' as SymbolId,
    LocationPin: 'map-pin-line-light' as SymbolId,
    LocationCrosshairs: 'crosshair-light' as SymbolId,
    Gift: 'gift-light' as SymbolId,
    House: 'house-light' as SymbolId,
    MagnifyingGlass: 'magnifying-glass-light' as SymbolId,
    Image: 'image-light' as SymbolId,
    Phone: 'phone-light' as SymbolId,
    Bars: 'list-light' as SymbolId,
    Heart: 'heart-light' as SymbolId,
    Xmark: 'x-light' as SymbolId,
    Comment: 'chat-text-light' as SymbolId,
    TruckFast: 'truck-light' as SymbolId,
    FaceSmile: 'smiley-light' as SymbolId,
    Bell: 'bell-light' as SymbolId,
    CalendarDays: 'calendar-light' as SymbolId,
    CircleInfo: 'info-fill' as SymbolId,
    Fire: 'fire-light' as SymbolId,
    Hand: 'hand-light' as SymbolId,
    Lemon: 'orange-light' as SymbolId,
    Bookmark: 'bookmark-simple-light' as SymbolId,
    Facebook: 'facebook-logo-light' as SymbolId,
    Instagram: 'instagram-logo-light' as SymbolId,
    Youtube: 'youtube-logo-light' as SymbolId,
    Google: 'google-logo-light' as SymbolId,
    Tripadvisor: 'tripadvisor' as SymbolId
  };

  // Check for legacy mapping first
  if (legacyMappings[iconName]) {
    return legacyMappings[iconName];
  }

  // Handle modern icon names with prefixes (e.g., 'ph:sun-light', 'logos:visa')
  // astro-svgs uses just the filename as the symbol ID
  if (iconName.includes(':')) {
    const [_prefix, name] = iconName.split(':');
    // Just return the name part (filename without directory)
    return name as SymbolId;
  }

  // Fallback: return as-is
  return iconName as SymbolId;
}

const symbolId = getSymbolId(icon);
---

<svg
  class={style}
  aria-hidden="true"
  xmlns="http://www.w3.org/2000/svg"
  focusable="false"
>
  <use href={`${file}#${symbolId}`} xmlns:xlink="http://www.w3.org/1999/xlink"
  ></use>
  {title && <title>{title}</title>}
</svg>
