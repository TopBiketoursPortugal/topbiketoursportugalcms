---
import type { Except } from 'type-fest';
import type { TourCollectionProps } from './tours-collection.astro';
import ToursCollection from './tours-collection.astro';

type Props = Except<TourCollectionProps, 'tourType'> & {
  tourType: 'package-tour';
};
---

<ToursCollection {...Astro.props} tourType="PackageTour" />
